import { Store } from '../Contracts/Store';
export declare class FileDriver implements Store {
    protected prefix: string;
    protected directory: string;
    constructor(directory: string, prefix?: string);
    get(key: string): Promise<any>;
    put(key: string, value: any, seconds: number): Promise<void>;
    increment(key: string, value?: number): Promise<number>;
    decrement(key: string, value?: number): Promise<number>;
    forever(key: string, value: any): Promise<void>;
    forget(key: string): Promise<void>;
    flush(): Promise<void>;
    getPrefix(): string;
    protected getPath(key: string): string;
    protected ensureDirectoryExists(filePath?: string): void;
    protected deleteFolderRecursive(dir: string): void;
    protected sha1(str: string): string;
}
//# sourceMappingURL=FileDriver.d.ts.map