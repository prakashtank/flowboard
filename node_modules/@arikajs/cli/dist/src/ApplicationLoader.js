"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ApplicationLoader = void 0;
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
class ApplicationLoader {
    static findRoot() {
        let current = process.cwd();
        while (current !== path_1.default.dirname(current)) {
            if (fs_1.default.existsSync(path_1.default.join(current, 'package.json'))) {
                return current;
            }
            current = path_1.default.dirname(current);
        }
        return process.cwd();
    }
    static async load() {
        const root = this.findRoot();
        const bootstrapPath = path_1.default.join(root, 'bootstrap', 'app.ts');
        if (fs_1.default.existsSync(bootstrapPath)) {
            // Register ts-node to handle .ts files
            require('ts-node/register');
            try {
                require(path_1.default.join(root, 'node_modules', 'tsconfig-paths', 'register'));
            }
            catch (e) {
                // Ignore if tsconfig-paths is not installed
            }
            try {
                // Require the app file, which should call createApp() and set the instance
                const appModule = require(bootstrapPath);
                const app = appModule.default || appModule;
                return {
                    root,
                    instance: app,
                    isArikaProject: true
                };
            }
            catch (error) {
                console.error(`Failed to load application at ${bootstrapPath}:`, error.message);
            }
        }
        return {
            root,
            isArikaProject: fs_1.default.existsSync(path_1.default.join(root, 'arika.json')) || fs_1.default.existsSync(path_1.default.join(root, 'kernel.ts'))
        };
    }
}
exports.ApplicationLoader = ApplicationLoader;
//# sourceMappingURL=ApplicationLoader.js.map