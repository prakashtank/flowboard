"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.QueueFailedCommand = void 0;
const console_1 = require("@arikajs/console");
const arikajs_1 = require("arikajs");
class QueueFailedCommand extends console_1.Command {
    constructor() {
        super(...arguments);
        this.signature = 'queue:failed';
        this.description = 'List all of the failed queue jobs';
    }
    async handle() {
        const db = (0, arikajs_1.app)().make('db');
        const failedJobs = await db.table('failed_jobs').get();
        if (failedJobs.length === 0) {
            this.info('No failed jobs found.');
            return;
        }
        const headers = ['ID', 'Connection', 'Queue', 'Failed At'];
        const rows = failedJobs.map((job) => [
            job.id,
            job.connection,
            job.queue,
            new Date(job.failed_at).toLocaleString()
        ]);
        this.table(headers, rows);
    }
}
exports.QueueFailedCommand = QueueFailedCommand;
//# sourceMappingURL=QueueFailedCommand.js.map