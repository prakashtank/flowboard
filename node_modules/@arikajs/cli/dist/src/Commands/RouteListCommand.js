"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.RouteListCommand = void 0;
const console_1 = require("@arikajs/console");
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
class RouteListCommand extends console_1.Command {
    constructor() {
        super(...arguments);
        this.signature = 'route:list';
        this.description = 'List all registered routes';
    }
    async handle() {
        const cwd = process.cwd();
        // Dynamically load the user's app Router/RouteRegistry.
        // We do this by importing their routes directly.
        // Arika standard apps have routes in: routes/web.ts and routes/api.ts
        const webRoutes = path_1.default.join(cwd, 'routes', 'web.ts');
        const apiRoutes = path_1.default.join(cwd, 'routes', 'api.ts');
        try {
            if (fs_1.default.existsSync(webRoutes)) {
                await Promise.resolve(`${webRoutes}`).then(s => __importStar(require(s)));
            }
            if (fs_1.default.existsSync(apiRoutes)) {
                await Promise.resolve(`${apiRoutes}`).then(s => __importStar(require(s)));
            }
        }
        catch (e) {
            this.error('Could not load application routes. Make sure you are running this command from the root of an ArikaJS application.');
            this.error(e.message);
            return;
        }
        // Now that the routes are imported, the RouteRegistry will have them.
        // But the CLI needs to import Router from the user's project to get the exact instance,
        // or since it's a singleton, we can grab it if it's installed locally.
        try {
            const { RouteRegistry } = await Promise.resolve(`${path_1.default.join(cwd, 'node_modules', '@arikajs', 'router')}`).then(s => __importStar(require(s)));
            const routes = RouteRegistry.getInstance().getRoutes();
            if (!routes || routes.length === 0) {
                this.info('No routes found.');
                return;
            }
            const tableData = routes.map((r) => {
                const methodStr = typeof r.method === 'string' ? r.method : r.method.join('|');
                let action = 'Closure';
                if (Array.isArray(r.handler) && r.handler.length === 2) {
                    action = `${r.handler[0].name || 'Controller'}@${r.handler[1]}`;
                }
                else if (r.handler && typeof r.handler === 'object' && r.handler.action) {
                    action = r.handler.action.name || 'Closure';
                }
                else if (typeof r.handler === 'function' && r.handler.name) {
                    action = r.handler.name;
                }
                return [
                    methodStr.toUpperCase(),
                    r.path,
                    r.name || '',
                    action,
                    r.middleware && r.middleware.length ? r.middleware.map((m) => m.name || 'Middleware').join(', ') : ''
                ];
            });
            this.table(['Method', 'URI', 'Name', 'Action', 'Middleware'], tableData);
            this.success(`\nFound ${routes.length} total routes.`);
        }
        catch (e) {
            this.error('Could not resolve @arikajs/router. Are you in an ArikaJS project?');
        }
    }
}
exports.RouteListCommand = RouteListCommand;
//# sourceMappingURL=RouteListCommand.js.map