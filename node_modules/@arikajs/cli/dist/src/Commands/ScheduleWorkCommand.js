"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ScheduleWorkCommand = void 0;
const console_1 = require("@arikajs/console");
const scheduler_1 = require("@arikajs/scheduler");
const path = __importStar(require("path"));
const fs = __importStar(require("fs"));
class ScheduleWorkCommand extends console_1.Command {
    constructor() {
        super(...arguments);
        this.signature = 'schedule:work';
        this.description = 'Start the scheduler worker';
    }
    async handle() {
        this.info('Starting scheduler worker...');
        try {
            const scheduler = this.container.make(scheduler_1.Scheduler);
            // Try to load user defined schedule
            await this.loadSchedule(scheduler);
            const worker = new scheduler_1.Worker(scheduler);
            await worker.start();
        }
        catch (error) {
            this.error(`Scheduler worker failed: ${error.message}`);
        }
    }
    async loadSchedule(scheduler) {
        const schedulePaths = [
            path.join(process.cwd(), 'app/Console/Kernel.ts'),
            path.join(process.cwd(), 'app/Console/Kernel.js'),
            path.join(process.cwd(), 'schedule.ts'),
            path.join(process.cwd(), 'schedule.js'),
        ];
        for (const filePath of schedulePaths) {
            if (fs.existsSync(filePath)) {
                // Determine if we need to use tsx/ts-node for .ts files
                const scheduleModule = await Promise.resolve(`${filePath}`).then(s => __importStar(require(s)));
                const defineSchedule = scheduleModule.default || scheduleModule;
                if (typeof defineSchedule === 'function') {
                    scheduler.define(defineSchedule);
                    return;
                }
            }
        }
    }
}
exports.ScheduleWorkCommand = ScheduleWorkCommand;
//# sourceMappingURL=ScheduleWorkCommand.js.map