import { Filesystem } from '../Contracts/Filesystem';
import { Storage as GCSStorage, Bucket } from '@google-cloud/storage';
import { Readable } from 'stream';
export declare class GCSDriver implements Filesystem {
    protected client: GCSStorage;
    protected bucket: Bucket;
    protected bucketName: string;
    protected urlEndpoint?: string;
    constructor(config: {
        projectId?: string;
        keyFilename?: string;
        bucket: string;
        url?: string;
        credentials?: any;
    });
    private getFile;
    put(path: string, contents: string | Buffer): Promise<void>;
    putStream(path: string, stream: Readable): Promise<void>;
    get(path: string): Promise<Buffer>;
    readStream(path: string): Readable;
    exists(path: string): Promise<boolean>;
    delete(path: string): Promise<void>;
    url(path: string): string;
    temporaryUrl(path: string, expiresAt: Date): Promise<string>;
    size(path: string): Promise<number>;
    lastModified(path: string): Promise<number>;
    mimeType(path: string): Promise<string>;
    copy(path: string, newPath: string): Promise<void>;
    move(path: string, newPath: string): Promise<void>;
}
//# sourceMappingURL=GCSDriver.d.ts.map