import { Rule } from '../Rule';
export declare class Confirmed implements Rule {
    private data;
    constructor(data: any);
    validate(value: any, attribute: string): boolean;
    message(): string;
}
//# sourceMappingURL=Confirmed.d.ts.map