import { Rule } from '../Rule';
export declare class In implements Rule {
    private allowedValues;
    constructor(allowedValues: any[]);
    validate(value: any): boolean;
    message(): string;
}
//# sourceMappingURL=In.d.ts.map