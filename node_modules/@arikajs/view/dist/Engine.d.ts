export interface ViewConfig {
    viewsPath: string;
    cachePath?: string;
    extension?: string;
    cache?: boolean;
    dev?: boolean;
}
export type ViewComposer = (data: any) => Promise<void> | void;
export type ViewHelper = (...args: any[]) => any;
export declare class Engine {
    private config;
    private sections;
    private sectionStack;
    private parentTemplate;
    private compiler;
    private templateLoader;
    private compiledFunctions;
    private sharedData;
    private composers;
    private helpers;
    private pushes;
    private pushStack;
    private componentStack;
    private slotStack;
    private fragmentMode;
    private onceKeys;
    constructor(config: ViewConfig);
    /**
     * Share data across all renders.
     */
    share(key: string, value: any): void;
    /**
     * Add a view composer.
     */
    composer(template: string, callback: ViewComposer): void;
    /**
     * Add a global helper.
     */
    helper(name: string, callback: ViewHelper): void;
    /**
     * Add a custom directive.
     */
    directive(name: string, handler: (expression: string | null, children?: string) => string): void;
    /**
     * Render a template file.
     */
    render<T = Record<string, any>>(template: string, data?: T, isInternal?: boolean): Promise<string>;
    /**
     * Render a fragment of a template.
     */
    renderFragment(template: string, fragment: string, data?: any): Promise<string>;
    private executeComposers;
    private renderTemplate;
    private loadFromDiskCache;
    private saveToDiskCache;
    private runCompiled;
    extend(parent: string): void;
    startSection(name: string, previousOutput: string): void;
    popSection(content: string): string;
    yield(name: string): string;
    startPush(name: string, previousOutput: string): void;
    startPrepend(name: string, previousOutput: string): void;
    endPush(content: string): string;
    endPrepend(content: string): string;
    private processEndPush;
    stack(name: string): string;
    startComponent(name: string, data: any, previousOutput: string): void;
    startSlot(name: string, previousOutput: string): void;
    endSlot(content: string): string;
    renderComponent(defaultSlotContent: string): Promise<string>;
    markOnce(id: string): void;
    hasOnce(id: string): boolean;
    isFragmentMode(): boolean;
    getFragment(): string | null;
}
//# sourceMappingURL=Engine.d.ts.map