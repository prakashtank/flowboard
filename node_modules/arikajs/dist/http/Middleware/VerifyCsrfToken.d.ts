import { Request, Response, Middleware } from '@arikajs/http';
export declare class VerifyCsrfToken implements Middleware {
    /**
     * The URIs that should be excluded from CSRF verification.
     */
    protected except: string[];
    /**
     * Handle the incoming request.
     */
    handle(request: Request, next: (request: Request) => Promise<Response>): Promise<Response>;
    /**
     * Determine if the request has a URI that should pass through CSRF verification.
     */
    protected inExceptArray(request: Request): Promise<boolean>;
    /**
     * Determine if the session and input tokens match.
     */
    protected tokensMatch(request: Request): Promise<boolean>;
    /**
     * Get the CSRF token from the request.
     */
    protected getTokenFromRequest(request: Request): Promise<string | null>;
    /**
     * Determine if the HTTP request uses a read-only method.
     */
    protected isReading(request: Request): boolean;
    /**
     * Determine if the application is running unit tests.
     */
    protected runningUnitTests(): boolean;
    /**
     * Add the CSRF token to the response cookies.
     */
    protected addCookieToResponse(request: Request, response: Response): Promise<Response>;
}
//# sourceMappingURL=VerifyCsrfToken.d.ts.map