"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.QueueServiceProvider = void 0;
const foundation_1 = require("@arikajs/foundation");
const queue_1 = require("@arikajs/queue");
class QueueServiceProvider extends foundation_1.ServiceProvider {
    /**
     * Register queue services.
     */
    async register() {
        // Register framework internal jobs
        try {
            const { SendQueuedMailable } = require('@arikajs/mail');
            queue_1.Worker.registerJob('SendQueuedMailable', SendQueuedMailable);
        }
        catch (e) {
            // Mail package might not be installed
        }
        this.app.singleton('queue', () => {
            const config = this.app.config().get('queue');
            const database = this.app.make('db');
            const manager = new queue_1.QueueManager(config, database);
            // Set static access
            queue_1.Queue.setManager(manager);
            return manager;
        });
        this.app.alias('queue', queue_1.QueueManager);
    }
    /**
     * Boot queue services.
     */
    async boot() {
        this.app.make('queue');
    }
}
exports.QueueServiceProvider = QueueServiceProvider;
//# sourceMappingURL=QueueServiceProvider.js.map