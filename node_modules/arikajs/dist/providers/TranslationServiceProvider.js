"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TranslationServiceProvider = void 0;
const foundation_1 = require("@arikajs/foundation");
const localization_1 = require("@arikajs/localization");
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
class TranslationServiceProvider extends foundation_1.ServiceProvider {
    /**
     * Register the translation service.
     */
    register() {
        this.app.singleton(localization_1.Translator, () => {
            const config = this.app.config();
            const locale = config.get('app.locale', 'en');
            const fallbackLocale = config.get('app.fallback_locale', 'en');
            const translator = new localization_1.Translator(locale, fallbackLocale);
            // Load translations from resources/lang
            this.loadTranslations(translator);
            return translator;
        });
    }
    /**
     * Load translations from the application's language directory.
     */
    loadTranslations(translator) {
        const langPath = path_1.default.join(process.cwd(), 'resources', 'lang');
        if (!fs_1.default.existsSync(langPath)) {
            return;
        }
        const locales = fs_1.default.readdirSync(langPath);
        for (const locale of locales) {
            const localePath = path_1.default.join(langPath, locale);
            if (!fs_1.default.statSync(localePath).isDirectory())
                continue;
            const files = fs_1.default.readdirSync(localePath);
            for (const file of files) {
                if (file.endsWith('.json')) {
                    const group = path_1.default.basename(file, '.json');
                    const content = JSON.parse(fs_1.default.readFileSync(path_1.default.join(localePath, file), 'utf-8'));
                    translator.load(locale, group, content);
                }
            }
        }
    }
}
exports.TranslationServiceProvider = TranslationServiceProvider;
//# sourceMappingURL=TranslationServiceProvider.js.map