"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ViewServiceProvider = void 0;
const foundation_1 = require("@arikajs/foundation");
const view_1 = require("@arikajs/view");
const path_1 = __importDefault(require("path"));
const ViewMiddleware_1 = require("../http/Middleware/ViewMiddleware");
class ViewServiceProvider extends foundation_1.ServiceProvider {
    /**
     * Register the service provider.
     */
    async register() {
        this.app.singleton(view_1.View, () => {
            const config = this.app.config();
            const viewsPath = config.get('view.paths', [
                path_1.default.join(this.app.getBasePath(), 'resources/views')
            ])[0];
            const cachePath = config.get('view.cache_path', path_1.default.join(this.app.getBasePath(), 'storage/framework/views'));
            const view = new view_1.View({
                viewsPath,
                cachePath,
                cache: config.get('app.env') === 'production'
            });
            view.helper('config', (key, defaultValue) => config.get(key, defaultValue));
            return view;
        });
        this.app.singleton('view', () => this.app.make(view_1.View));
        this.app.singleton(ViewMiddleware_1.ViewMiddleware, () => {
            return new ViewMiddleware_1.ViewMiddleware(this.app.make(view_1.View));
        });
    }
    /**
     * Boot the service provider.
     */
    async boot() {
        //
    }
}
exports.ViewServiceProvider = ViewServiceProvider;
//# sourceMappingURL=ViewServiceProvider.js.map